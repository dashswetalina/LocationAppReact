import React from 'react';
import classes from './Footer.module.css';


const footer = (props) => {
    return(
        <footer className={classes.Footer}>
         @Copyright Kaiser Permanente
        </footer>
    );
}

export default footer;